# VK Comments Monitor Bot

Telegram-бот для мониторинга комментариев в группах ВКонтакте по ключевым словам.

## Возможности

- 🔍 Мониторинг комментариев в группах ВКонтакте
- 📊 Автоматическая проверка по расписанию
- 📁 Экспорт данных в Excel
- 💬 Уведомления в Telegram чаты
- 🎯 Поиск по ключевым словам
- 📈 Статистика найденных комментариев

## Структура проекта

```
PythonProject/
├── bot/
│   ├── __init__.py
│   ├── main.py              # Главный файл запуска
│   ├── config.py            # Конфигурация
│   ├── handlers/             # Обработчики команд и сообщений
│   │   ├── commands.py
│   │   ├── messages.py
│   │   └── keyboards.py
│   ├── database/            # Работа с базой данных
│   │   └── db.py
│   ├── services/            # Сервисы
│   │   ├── vk_service.py
│   │   ├── vk_checker.py
│   │   ├── excel_service.py
│   │   └── notification_service.py
│   └── utils/               # Утилиты
│       ├── helpers.py
│       └── status.py
├── data/                     # Данные (БД, Excel файлы)
├── .env.example             # Пример конфигурации
├── requirements.txt         # Зависимости Python
├── Dockerfile               # Docker образ
├── docker-compose.yml       # Docker Compose конфигурация
└── README.md               # Документация
```

## Миграция со старой версии

Если у вас была старая версия бота (`xpom_bot.py`), данные автоматически будут использованы из директории `data/`. Старый файл `xpom_bot.py` можно удалить или переместить в архив после успешного запуска новой версии.

## Установка и запуск

### Локальный запуск

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd PythonProject
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Заполните токены в `.env`:
```env
VK_TOKEN=your_vk_token_here
TELEGRAM_TOKEN=your_telegram_bot_token_here
```

6. Запустите бота:
```bash
python -m bot.main
# или
python run.py
```

### Запуск с Docker

1. Создайте файл `.env`:
```bash
cp .env.example .env
```

2. Заполните токены в `.env`

3. Запустите с Docker Compose:
```bash
docker-compose up -d
```

4. Просмотр логов:
```bash
docker-compose logs -f
```

5. Остановка:
```bash
docker-compose down
```

## Получение токенов

### VK Token

1. Перейдите на https://vk.com/apps?act=manage
2. Создайте новое приложение
3. Получите токен доступа с правами: `groups`, `wall`, `offline`

### Telegram Bot Token

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

## Использование

1. Запустите бота и отправьте команду `/start`
2. Добавьте группы ВКонтакте через кнопку "Добавить группу"
3. Добавьте ключевые слова через кнопку "Добавить ключевое слово"
4. Добавьте чаты для уведомлений через кнопку "Добавить чат"
5. Бот будет автоматически проверять комментарии каждые 10 минут (настраивается в `.env`)

## Команды бота

- `/start` - Запуск бота и показ клавиатуры
- `/keyboard` - Показать клавиатуру снова
- **Статус** - Показать текущий статус бота
- **Добавить группу** - Добавить группу ВК для мониторинга
- **Добавить ключевое слово** - Добавить ключевые слова для поиска
- **Проверить сейчас** - Запустить проверку вручную
- **Экспорт в Excel** - Получить Excel файлы с данными

## Настройки

Все настройки находятся в файле `.env`:

- `CHECK_INTERVAL` - Интервал проверки в секундах (по умолчанию 600)
- `POSTS_COUNT` - Количество постов для проверки (по умолчанию 20)
- `COMMENTS_COUNT` - Количество комментариев для проверки (по умолчанию 100)
- `LOG_LEVEL` - Уровень логирования (DEBUG, INFO, WARNING, ERROR)

## Лицензия

MIT

## Автор

@ffrostnova

